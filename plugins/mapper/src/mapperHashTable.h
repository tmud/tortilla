#pragma once

/*#include "mapperObjects.h"

class MapperHashTable
{
public:
    MapperHashTable();
    ~MapperHashTable();
    void addRoom(Room* room);
    void deleteRoom(Room* room);       
    void findRooms(const RoomData& room, std::vector<Room*> *vr);
private:
    typedef std::vector<Room*> rooms_list;
    typedef std::vector<Room*>::iterator rooms_list_iterator;
    std::map<uint, rooms_list*> rooms;
    typedef std::map<uint, rooms_list*>::iterator iterator;    
};
*/